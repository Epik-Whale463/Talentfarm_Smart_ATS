# Production environment configuration
FLASK_ENV=production

# Security keys
SECRET_KEY=REPLACE_ME_SECRET_KEY
JWT_SECRET_KEY=REPLACE_ME_JWT_SECRET_KEY

# PostgreSQL Database configuration
DB_USER=REPLACE_ME_DB_USER
DB_PASSWORD=REPLACE_ME_DB_PASSWORD
DB_HOST=REPLACE_ME_DB_HOST
DB_PORT=REPLACE_ME_DB_PORT
DB_NAME=REPLACE_ME_DB_NAME

# Remove/comment this out since we're using PostgreSQL now
# DATABASE_URL=sqlite:///instance/ats.db

# API Keys
MISTRAL_API_KEY=REPLACE_ME_MISTRAL_API_KEY
GROQ_API_KEY=REPLACE_ME_GROQ_API_KEY

# GitHub OAuth
GITHUB_CLIENT_ID=REPLACE_ME_GITHUB_CLIENT_ID
GITHUB_CLIENT_SECRET=REPLACE_ME_GITHUB_CLIENT_SECRET
GITHUB_CALLBACK_URL=REPLACE_ME_GITHUB_CALLBACK_URL

# Vector Database (Qdrant)
QDRANT_URL=REPLACE_ME_QDRANT_URL
QDRANT_API_KEY=REPLACE_ME_QDRANT_API_KEY
EMBEDDING_MODEL=REPLACE_ME_EMBEDDING_MODEL
